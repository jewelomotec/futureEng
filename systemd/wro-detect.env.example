# Copied to /etc/default/wro-detect by the install script (optional overrides).
# systemd EnvironmentFile cannot change ExecStart paths — edit the .service for that.

WRO_HEADLESS=1
PYTHONUNBUFFERED=1
